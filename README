Librerie istallate:
- Pandas (creazione e gestione di dataframe) -> pip3 intall pandas
- Matplotlib (visualizzazione dei dati) -> pip3 install matplotlib
- Scipy (Analisi indici statistici) -> pip3 install scipy
- Scikit Learn (preprocessing e modelli Machine Learning) -> pip3 install scikit-learn
- Seaborn (funzionalità grafiche e dataset, alternativa a Matplotlib) -> pip3 install seaborn
- Kneed (per trovare il miglior numero di cluster) -> pip3 install kneed
- Pycountry (informazione globale sui paesi) -> pip3 install pycountry
- Pymysql (connessione a database MySQL) -> pip3 install pymysql
- Request (gestione delle richieste HTTP) -> pip3 install requests
- BeautifulSoup4 (analisi dei documenti HTML e XML / Web Scraping) -> pip3 install beautifulsoup4